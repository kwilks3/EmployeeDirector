import React from "react";
import { Form } from "react-bootstrap";

export default function Search() {
  return (
    <Form>
      <Form.Control placeholder="Search by First Name" />
    </Form>
  );
}
